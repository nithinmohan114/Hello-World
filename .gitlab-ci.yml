services:
  - postgres:latest

variables:
  POSTGRES_DB: helloworld_test
  POSTGRES_USER: posgtres
  POSTGRES_PASSWORD: postgrespasswordforentri

Test:
  variables:
    DATABASE_URL: postgres://postgres:postgrespasswordforentri@postgres:5432/helloworld_test
    # DJANGO_SETTINGS_MODULE: entri.entri.settings.base 
  script:
  - apt-get update -qy
  - apt-get install -y python-dev python-pip
  - pip install django
  - pip install pytest
  - pytest my_app/test_sample.py
